{
  "name": "alpha",
  "version": "1.0.0",
  "description": "Social media site",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "start:client": "npm --prefix client run dev",
    "start:server": "npm --prefix server run dev",
    "start": "npm run start:server",
    "build:client": "npm --prefix client run build",
    "postinstall": "npm install --prefix client && npm run build:client"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cookie-parser": "^1.4.7",
    "react-router-dom": "^6.14.1"
  }
}
